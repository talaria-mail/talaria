const state = {
  token: null
}

const getters = {
  getAuthToken: (state) => state.token
}

const actions = {
}

const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
};
